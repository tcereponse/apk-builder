import { type ReactNode } from 'react'

export function ContactFormData({ children }: { children?: ReactNode }) {
  return <>{children}</>
}

export function ContactFormSubmissionResponse({ children }: { children?: ReactNode }) {
  return <>{children}</>
}

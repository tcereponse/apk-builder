- Logique pure de calcul de score selon les règles classiques (1 ligne = 100, 2 = 300, 3 = 500, 4 = 800) avec multiplicateur de niveau.

Performance TargetsFirst Contentful Paint: < 1.0s (Vite + index.html minimal)

Time to Interactive: < 1.5s (code splitting automatique via React.lazy)

Bundle Size: < 50KB gzip (hors React et React-DOM)

Frame Rate: 60fps constant pendant le jeu (requestAnimationFrame + delta-time)